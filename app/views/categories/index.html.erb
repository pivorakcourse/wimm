<br>
<div>
  <%= link_to "Add category", new_category_path, class: "button button secondary" %>
  <% @categories.each do |category| %>
      <ul><%= category.name %><br>
        <%=link_to 'Edit', edit_category_path(category) %>
      </ul>

  <% end %>  
</div>

