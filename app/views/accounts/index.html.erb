<div class="row">
  <div>
    <% @accounts.each do |account| %>
      <ul><%= link_to account.name, edit_account_path(account) %></ul>
    <% end %>
  </div>

  <div>
     <%= link_to "Add", new_account_path, class: "button button-secondary" %>
  </div>
</div>
